## 2026-08-02 - Balalaika training foundation

- Decision: Use Accelerate as the primary distributed-training interface, with a native DDP fallback retained for environments where Accelerate is unavailable or unsuitable.
- Scope: This foundation validates the two-stage curriculum contract and keeps only the configuration fields needed by the initial pipeline boundary.
- Validation: `rtk uv sync --extra dev --extra balalaika`, the focused Balalaika tests, formatting checks, and the full `tests/` suite completed successfully.

## 2026-08-02 - GigaAM adapter and validation ledger

- Decision: Load only the named `gigaam-v3-rnnt` model from its already pinned local directory, and verify CUDA availability before opening a session; the loader is never given a remote repository identifier.
- Decision: Store each benchmark ID as an independently locked, atomically replaced JSON record so distributed ranks can safely own disjoint IDs and a crash cannot publish a partial record.
- Assumption: Generation and ASR fingerprints identify all deterministic inputs supplied by the later evaluator; WAV content hashes are recomputed before reuse.
- Validation: RED collection failed while the modules were absent; focused adapter/ledger tests are green (16 passed), `py_compile`, Black, and `git diff --check` pass; the full `tests/` suite passes (134 tests).

## 2026-08-02 - GigaAM and ledger Fix Round 1

- Decision: A claim is now an opaque token plus monotonic per-item epoch and rank. Every generation/ASR/failure mutation requires the live claim under that item's advisory lock; generation retains the claim through ASR, while terminal stage results release it.
- Decision: Completion queries require the evaluator's current deterministic inputs and seed. Bulk completion receives an ID-to-`(inputs, seed)` mapping or callback, so stored inputs alone can never establish reuse.
- Decision: After `onnx_asr.load_model`, inspect the local package's documented adapter-to-ASR ownership (`adapter.asr._encoder`, `_decoder`, `_joiner`) and require every discovered model session to report CUDA as primary on the requested device. CPU-only pre/post-processing is not inspected as a model session.
- Validation: Targeted RED tests covered missing mutation claims, token/rank/lease takeover, current-input completion, active CPU fallback, unlisted `.onnx`, directory fsync, and interrupted pending attempt caps. GREEN: 24 focused tests and 139 full-suite tests passed; Black, compile, and diff checks passed.

## 2026-08-02 - Rank-zero W&B validation tracking

- Decision: Worker ranks receive a `NullRunManager`, so only the main process dynamically imports, initializes, logs, or finishes W&B. Memorization, stage 1, and stage 2 persist independent UUIDs before `wandb.init(..., resume="allow")`, while sharing the configured experiment group.
- Decision: A validation handoff is immutable and rejects any boundary other than 2,000 unique scored IDs and four existing local WAVs. Each upload builds a fresh W&B table and retains the selection-owned audio ID order in captions.
- Decision: Write an atomic pending boundary manifest before `run.log`; fsync the W&B run directory and atomically mark it complete only after `run.log` returns. Completed matching manifests suppress duplicate uploads on resume; failed or pending boundaries remain incomplete and retryable.

## 2026-08-02 - W&B validation tracking Fix Round 1

- Decision: A completed or pending boundary is identified by a canonical, atomically written and read-back full snapshot rather than a short metadata manifest. The snapshot holds every logged table row, metrics/counts/categories, progress, fingerprints, timing/failure values, and ordered local audio paths/hashes/captions; the manifest stores its fingerprint.
- Decision: Validation recomputes aggregates from every `ItemScore`, and binds the Task-5 selection fingerprint plus ordered audio IDs to the payload. This prevents aggregate drift, wrong example ordering, changed WAVs, and score/benchmark normalized-gold divergence from reaching W&B.
- Decision: Existing W&B state can resume only with the persisted config fingerprint. Local recovery is durable, but W&B backend acknowledgement is not made transactional; a crash after `run.log` remains safely retryable from the verified snapshot.

## 2026-08-02 - Exact fractional scheduling and LoRA-only checkpoints

- Decision: Epoch geometry floors the row count to complete global microbatches, then floors again to complete accumulation groups. The combined row loss is recorded as dropped samples, and geometries with fewer than eight optimizer steps are rejected because they cannot represent eight unique boundaries.
- Decision: Validation boundaries are stage-relative completed optimizer steps computed as integer ceilings for eighths. `TrainingProgress` persists the emitted-boundary cursor separately from optimizer progress, preventing step-zero, resume, and epoch-end duplicate emissions.
- Decision: Same-stage saves use temporary sibling directories and Accelerate save/load pre-hooks. The save hook writes only `lora_` tensors and clears Accelerate's model-weight list; optimizer, scheduler, RNG, and registered progress remain owned by `save_state`/`load_state`. Every file is hashed before atomic publication and the latest pointer carries the immutable metadata fingerprint.
- Decision: Stage transition is a separate adapter-only path. It accepts only the final stage-1 boundary, validates target-compatible identities, never receives an Accelerator, and optionally resets stage-relative progress while preserving the cross-stage global step.
- Validation: Focused RED failed at collection while both modules were absent. Focused GREEN covers 27 schedule/checkpoint cases; the full `tests/` suite passes 194 tests. Black check, `py_compile`, and `git diff --check` also complete cleanly.

## 2026-08-02 - LoRA checkpoint hardening Fix Round 1

- Changed: Checkpoint schema 2 binds exact Accelerate optimizer, scheduler, registered-progress, and per-rank RNG filenames/counts. RNG file hashes are embedded in checkpoint metadata and also covered by the complete file manifest.
- Decision: Only Accelerate `NO`, `MULTI_CPU`, and `MULTI_GPU` backends are supported. DDP-prefixed hook state is normalized against the unwrapped model's complete LoRA key set; FSDP, DeepSpeed, incomplete adapters, and partial expected identities are rejected before state mutation.
- Decision: Boundary metadata now includes optimizer-step epoch geometry, state-object counts, and stage-start global progress. Checkpoints must occur on an exact eighth boundary with aligned microsteps, epoch/sampler state, and stage/global counters. Stage 2 imports the verified stage-1 global step before resetting all stage-relative counters.
- Decision: Same-stage resume safely preflights registered progress against metadata. Any later Accelerate load failure poisons the manager and requires a process restart rather than permitting use of potentially partial model/optimizer/RNG state.
- Validation: Four focused RED groups reproduced DDP/backend/adapter, transition/identity, exact-state/progress, and late-restore failures (5, 3, 7, and 2 failures respectively). GREEN passes 48 focused tests and 215 full-suite tests. A real single-rank CPU Gloo `DistributedDataParallel` roundtrip confirms `module.*` normalization; Black, `py_compile`, and diff checks pass.

## 2026-08-02 - Accelerate runtime and synchronized microbatch probe

- Decision: Accelerate is the only implementation under the initial spike. Native DDP will be added only if the same Accelerate model-behavior, synchronization, or restore failure remains after at most two minimal runtime/probe fixes; transient shared-GPU resource contention is not a fallback trigger.
- Scope: The real one- and eight-process checks use only a tiny synthetic LoRA module and synthetic sample IDs. They do not load VoxCPM, memorization data, or a production training path.
- Changed: The first eight-process restore exposed that Accelerate 1.14.0 writes shared model/optimizer state on the main process but `save_state` does not end with a barrier. Non-main ranks returned early and ranks 2, 4, and 7 observed a temporarily absent `optimizer.bin`. The first and only runtime/probe fix adds `wait_for_everyone()` after the public `save_state`; its targeted RED observed `[save]`, GREEN observes `[save, barrier]`, and the identical eight-process retry passed.
- Attempts: Two initial `/usr/local/bin/accelerate` launches failed before runtime construction because the global Python first lacked the editable `voxcpm` package and then `argbind`. A temporary diagnostic import bootstrap was removed. All authoritative commands use the project environment: `rtk .venv/bin/accelerate launch --num_processes 1 scripts/smoke_balalaika_accelerate.py --mode train-checkpoint` and the same command with `--num_processes 8`. The eight-process launcher found port 29500 occupied and safely selected an open standalone port; no external process was altered.
- Validation: RED initially failed collection because `runtime.py`/`probe.py` were absent. Focused GREEN passes 10 tests. The pre-change Balalaika baseline passes 152 tests and the final full repository test directory passes 218 tests. Bare `pytest -q` is not a valid repository command because `scripts/test_pick_runtime_dtype.py` exits during collection; `pytest tests/ -q` is the established full suite. Black, `py_compile`, and diff checks pass.
- Validation: Canonical one GPU passed with IDs `[0,1,2,3]`, accumulation `[false,true]`, optimizer step 1, restored next position 4, and 16.267 MiB peak. Canonical eight GPU passed with disjoint partitions `[[0,1,16,17],[2,3,18,19],[4,5,20,21],[6,7,22,23],[8,9,24,25],[10,11,26,27],[12,13,28,29],[14,15,30,31]]`; every rank reported accumulation `[false,true]`, LoRA gradient `[-0.0039180964,-0.0026081726]`, optimizer step 1, restored next position 32, and 16.269 MiB peak. Probe microbatch 2, reference update, and checkpoint restore passed on every rank. Logs and checkpoints are in `.superpowers/sdd/2026-08-02-balalaika-two-stage-lora-training/task-10-artifacts/`.
- Environment: Python 3.14.0, Accelerate 1.14.0, PyTorch 2.10.0+cu128, CUDA runtime 12.8, cuDNN 9.10.2, NCCL 2.27.5, NVIDIA driver 570.153.02, eight NVIDIA GeForce RTX 5090 GPUs with 32607 MiB each. Before the spike, shared usage ranged from 15119 to 27231 MiB; the synthetic spike allocated only about 16.3 MiB per process.
- Decision: Accelerate primary path passed; native DDP not implemented.

## 2026-08-02 - Accelerate probe Fix Round 1

- Changed: The original probe step received the runtime and prepared model, so a rank-local OOM could branch to the status gather while peer ranks entered DDP backward synchronization. It also used `runtime.accumulate`, advancing Accelerate's accumulation cursor during a supposedly reversible probe.
- Decision: The second and final permitted Accelerate fix makes probing a strictly local pre-`prepare` operation. The step receives only the verified unwrapped model and sample, uses ordinary local autograd/optimizer operations, and performs exactly one cross-rank status collective per candidate. Passing a wrapped model is rejected before work begins.
- Validation: Unit RED produced six expected failures because the old probe required `step_fn.model` and passed the runtime into the candidate. Focused GREEN passes 12 runtime/probe tests, including wrapped-model rejection, coherent local/remote non-OOM termination, one status gather with no barrier, an OOM candidate followed by a later successful candidate, complete rollback, and an unchanged fake accumulation cursor.
- Validation: `rtk timeout 240s .venv/bin/accelerate launch --num_processes 8 --main_process_port 0 scripts/smoke_balalaika_accelerate.py --mode probe-rank-fault` exited 0 before timeout. Exactly rank 7 injected CUDA OOM after local backward for candidate 2 while peers completed local optimizer work; every rank selected prior candidate 1, then reported normal prepared accumulation `[false,true]` and optimizer step 1. No hang or cursor shift occurred.
- Validation: The reordered original smoke passed under `rtk timeout 180s .venv/bin/accelerate launch --num_processes 1 ... --mode train-checkpoint` and `rtk timeout 240s .venv/bin/accelerate launch --num_processes 8 --main_process_port 0 ... --mode train-checkpoint`. The full CPU suite passes 220 tests. Fix-round logs are stored beside prior Task 10 artifacts.
- Decision: The second allowed minimal Accelerate fix passed the rank-fault and original smokes; Accelerate remains primary and native DDP remains unimplemented.
- Follow-up: The minor smoke-test RNG assertion finding remains deferred as directed; production CPU/local-CUDA RNG rollback remains covered.

## 2026-08-02 - Distributed Balalaika evaluator

- Decision: Keep the evaluator's production boundary dependency-injected: the runtime, Task-5 selection, boundary-local Task-7 ledger, per-rank ASR factory, and rank-aware tracking manager are supplied by the trainer. The evaluator derives and validates all item inputs from the current checkpoint, boundary, benchmark rows, prompt files, and selection fingerprint.
- Decision: Treat aggregate JSON/JSONL as a deterministic durable snapshot. W&B logging must finish before `validation-complete.json` is atomically published; only a matching durable completion permits retention.
- Decision: Retain the four selection-owned WAVs in a compact per-boundary archive before pruning older completed boundaries' full `wavs/` trees. Incomplete boundaries are never pruned.
- Changed: The evaluator restores the unwrapped model and retained AudioVAE modes only after the per-rank ASR adapter has been closed. Generation temporarily attaches the AudioVAE, while every ledger mutation receives the live token/epoch claim.
- Validation: RED was the absent `evaluation.py` import and the absent smoke `validation` CLI. GREEN passes 9 focused evaluator tests, 66 evaluator/ledger/metrics/tracking/runtime tests, and all 229 repository tests. Black, `py_compile`, and `git diff --check` pass.
- Validation: The final-tree required no-network smoke passed with eight real Accelerate processes and 32 synthetic items. Every rank generated four unique position-modulo IDs, released one fake ASR session, and verified the completion; rank 0 published one aggregate, one completion, and one tracking record. Artifacts are under `.superpowers/sdd/2026-08-02-balalaika-two-stage-lora-training/task-11-artifacts/validation-8x-1785702862763622381`.
- Environment: Two earlier 240/150-second diagnostics used an allocated PTY and timed out before script entry because the Accelerate parent was job-control stopped (`T` state). The unchanged required command without a PTY exited 0 in about 112 seconds; this was a harness issue, not an evaluator fix.

## 2026-08-02 - Distributed evaluator Fix Round 1

- Changed: ASR reacquires a caller-current ledger claim immediately before every mutation attempt and retains the generation claim only when no replacement is available. If neither claim is available, evaluation fails deterministically instead of writing with an expired lease.
- Decision: Snapshot the training process's CPU and rank-local CUDA RNG before validation and restore both in the outer cleanup path, after ASR and model/AudioVAE teardown, on success, complete resume, and exception.
- Decision: Retention treats every historical completion as untrusted. It validates the canonical completion and exact schemas/identities, aggregate hashes and recomputed metrics, every item/source-WAV hash, and exactly four ordered unique fixed audio records. All missing retained boundaries are staged and verified before directories are atomically published; pruning starts only after every retained boundary re-verifies, so corruption or copy failure preserves all full WAV trees.
- Changed: A completed-boundary resume now has one collective outcome. Rank zero durably publishes retention success or a serialized failure, every rank crosses the same barrier, and every rank validates and consumes the same status before returning or raising.
- Validation: Eight targeted regressions reproduced stale-claim, RNG, retention-trust/atomicity, and resume-collective failures before the fixes. GREEN passes all 17 evaluator tests and all 237 tests under the established `pytest tests/ -q` repository boundary; Black, `py_compile`, and `git diff --check` pass. The exact eight-rank/32-item validation smoke exits 0 with four disjoint items per rank, one aggregate/completion/tracking publication on rank zero, and ASR release on every rank.

## 2026-08-02 - Two-stage LoRA trainer

- Changed: Task 12 requires signal/exception recovery between validation boundaries, while Task 9 intentionally allowed only exact eighth-boundary checkpoints. Recovery therefore uses a distinct checkpoint kind with complete-accumulation geometry; it does not weaken normal boundary validation or the final-stage1 adapter transition.
- Decision: Signal handling is cooperative: record the signal, finish the current accumulation group and real optimizer step, synchronize the stop decision, save recovery state, and stop before another microbatch. A distributed failure that cannot reach this coordinated safe point must retain the last durable checkpoint and require restart instead of attempting a divergent-rank save.
- Decision: Coordinate rank-local data loading, forward, and optimizer phase status before entering the next distributed operation. A failed backward collective is never followed by a recovery barrier/save; the error instead identifies the last durable checkpoint and requires a fresh process.
- Decision: A boundary transaction is checkpoint, evaluator, then atomic completion marker. Boundary-checkpoint resume runs the evaluator before another microbatch; recovery resume trusts the persisted last-completed boundary cursor and does not falsely rerun it. Stage-2 same-stage recovery obtains its stage-start global step from verified metadata and does not require the old stage-1 checkpoint.
- Changed: Import Transformers scheduler and VoxCPM2 model classes only at their execution boundaries. This keeps package collection compatible with existing lightweight module stubs without changing production loading.
- Validation: Trainer RED failed at collection while `trainer.py` was absent. Recovery RED exposed the missing `save_recovery` API; later RED cases covered rank-local loader failure coordination, stage-2 recovery-only resume, and current-identity approval binding. GREEN passes 85 focused trainer/checkpoint/evaluator/runtime/probe tests and all 256 repository tests. Black, `compileall`, and `git diff --check` pass. No real VoxCPM2 weights, proprietary data, GPU training, or network access was used.

## 2026-08-02 - Two-stage LoRA trainer Fix Round 1

- Changed: Disable Accelerate's scheduler-with-optimizer multiplication because the trainer explicitly owns one scheduler advance per real global update. A real 8-rank CPU/Gloo Accelerate regression confirms 3 optimizer updates produce 3 scheduler updates, not 24, through one joint prepare.
- Decision: Persisted progress describes completed real updates; a separate runtime accumulation cursor describes in-flight backwards. Recovery requires cursor zero after a non-skipped optimizer/scheduler/progress transaction. Loader or forward faults inside a group, optimizer/collective faults, and skipped-update-only states require restart or another real update first.
- Decision: Treat exact-boundary recovery as potentially pre-validation. Rank zero verifies the durable completion marker and publishes the result; missing, corrupt, or mismatched markers rerun and atomically replace validation completion before training continues.
- Changed: Rank-local state-save, checkpoint finalization, and boundary-marker errors are converted into one gathered phase outcome before any rank raises. A failure of that outcome collective is typed as restart-required so the trainer never attempts another barrier or recovery save.
- Decision: Reject any configured curriculum other than stage 1 = 2 epochs at 1e-4 and stage 2 = 3 epochs at 5e-5 before model setup. Seedable loaders receive the persisted sampler epoch before both initial and replay iterators, while the next real epoch increments once.
- Validation: Targeted runtime/checkpoint/trainer tests pass 83 cases; focused trainer/checkpoint/evaluator/runtime/probe integration passes 107; the full `tests/` suite passes 278 with 5 warnings. Final real eight-process evidence reports one prepare, a deterministic same-epoch replay after one skipped synchronized attempt, next sampler epoch +1, 4 attempts, 3 real optimizer steps, and 3 scheduler steps on every rank.

## 2026-08-02 - Two-stage LoRA trainer Fix Round 2

- Changed: A recovery created immediately after a completed validation boundary now carries the source boundary checkpoint name, fingerprint, and exact evaluator boundary identity in its own fingerprinted metadata.
- Decision: Recovery resume suppresses validation only after rank zero fully verifies the referenced immutable checkpoint at the exact restored progress and verifies its matching durable marker. The result is gathered once; absent, malformed, corrupt, or mismatched proof falls back to evaluator completion rather than being trusted.
- Validation: The production-shaped regression failed before the fix because step 10 appeared in the resumed evaluator (`assert [10] == []`), then passed after the proof bridge. Trainer/checkpoint coverage passes 76 tests and broader trainer/checkpoint/evaluator/runtime/probe coverage passes 108 tests, both with 4 warnings. Changed-file Black, compileall, and diff checks pass.

## 2026-08-02 - Four-example memorization and approval gate

- Decision: The memorization runner reads only the published four-sample selection manifest and its extracted WAVs, repeats those identities deterministically, and reuses the trainer's model builder, batch processor, weighted loss, optimizer/scheduler, runtime accumulation order, skipped-step replay, and `CheckpointManager.save_recovery` path. It does not rewrite or resample the published corpus artifacts.
- Decision: Rank zero alone reattaches the retained AudioVAE for prompt-free generation, logs all four reference/generated pairs to a distinct memorization W&B run, and publishes the stopped result. Worker ranks share the persisted W&B run identity and consume the rank-zero result after a barrier.
- Decision: GigaAM is diagnostic-only. Constructor, context-entry, transcription, and malformed-result failures become logged error diagnostics and cannot approve or reject the memorization result.
- Decision: Manual approval is a separate function. Its atomic record binds the approver and timestamp, full current base/index/selection/LoRA identity, W&B run/completion, result hash, selection manifest, all eight audio artifacts, and every checkpoint file. Verification requires the complete expected fingerprint set; stage 1 now includes selection identity in its pre-model approval check.
- Validation: RED first failed collection because `memorization.py` was absent. Additional RED cases reproduced skipped-update exhaustion, missing AudioVAE generation attachment, gating ASR setup/transcription failures, partial expected approvals, and a selection-mismatched stage reaching model setup. GREEN passes 23 focused tests, 142 relevant memorization/trainer/tracking/selection/checkpoint/runtime/ASR tests, and all 302 repository tests. No real model/data download, GPU memorization, network call, or stage training was run.

## 2026-08-02 - Memorization approval gate Fix Round 1

- Changed: Every rank-zero-only phase now publishes a durable failure record and gathers one phase outcome before any rank proceeds. Per-rank batch-audit publication has its own all-rank outcome, and the result is published only after rank zero verifies the combined attempted and successful-contribution identities. This removes the former unconditional final-barrier deadlock path.
- Decision: Reject geometry that cannot expose all four selected identities before model or tracking setup. The loader uses a seeded `RandomSampler` over the fixed-size repeated dataset; audit identities come from the actual prepared batch `dataset_ids`, not a synthesized sequence. Skipped optimizer attempts remain attempted but are excluded from successful contributions.
- Decision: Memorization is approval-eligible only in W&B online mode. The local completion binds the exact four-pair payload (run ID, step, identity, text, diagnostic, and reference/generated audio paths and hashes) with SHA-256. It explicitly records that W&B supplies no server-side receipt in this workflow, so the evidence proves local client `log`/`finish` return and payload identity, not remote ingestion.
- Validation: Focused RED reproduced four rank-zero propagation failures, three geometry/coverage failures, deterministic-shuffle failure, and six W&B mode/receipt/payload failures. GREEN passes 35 memorization tests, 154 relevant tests, and all 314 repository tests. Black, Flake8 with the Black-compatible 120-column boundary, compileall, and `git diff --check` pass.

## 2026-08-03 - Balalaika operator CLI and runbook (Task 14 Phase A)

- Decision: Keep `argparse` and process/artifact guards in the thin CLI, with typed injected command services in `workflow.py`. The production service delegates pinning to the existing `hf`-CLI Hub module and delegates preparation, selection, memorization, approval, training, checkpoint verification, and evaluation construction to their typed pipeline interfaces.
- Decision: Derive `BuildExpectations` from the immutable `/workspace/balalaika_proprietary_v2/verification.json` shard inventory and the trusted combined-sidecar metadata instead of duplicating 519 hashes in YAML. Preparation still verifies every source tar, ROVER archive, and combined sidecar byte before publishing outside the corpus.
- Decision: Large `memorize`, `train`, and `validate` launches require `WORLD_SIZE=8`; an explicit synthetic smoke seam may bypass the guard, while the production training service directs smoke verification to `scripts/smoke_balalaika_accelerate.py` so `--smoke` cannot accidentally traverse the real corpus.
- Decision: Stage 1 requires the artifact-bound memorization approval before model or dataset setup. Stage 2 accepts an explicit final adapter or resolves `stage1/checkpoints/latest.json`, then requires a stage-1 epoch-2 boundary-8 checkpoint and adapter before routing.
- Decision: The checked-in config contains no credentials. Hugging Face authentication remains in `hf`/`HF_TOKEN`; W&B entity, project, group, directory, and mode remain configurable, with online mode selected because memorization approval requires human inspection of uploaded evidence.
- Changed: Added a package marker beneath `tests/training/balalaika` because the required nested `test_cli.py` otherwise collided with the pre-existing top-level `tests/test_cli.py` under pytest's default import mode.
- Validation: CLI RED failed during collection with the expected absent-module error. Subsequent RED covered a changed pinned Hub artifact being accepted by audit. GREEN passes 16 focused CLI tests, 164 relevant Balalaika integration tests, and the complete `tests/` suite (`330 passed`, 5 known TorchCodec warnings). The installed `voxcpm-balalaika --help` exposes all seven commands; changed-file Black and Flake8, `compileall -q src scripts`, `uv lock --offline`, and `git diff --check` pass.
- Scope: Phase A used only synthetic/fake seams. It did not run real `pin`, real `prepare`, production `audit`, any GPU smoke, memorization, approval, or stage training. Production counts, index bytes/runtime/fingerprint, and final 1-rank/8-rank smoke evidence remain deliberately deferred to Task 14 Phase B.

## 2026-08-03 - Balalaika production qualification (Task 14 Phase B)

- Changed: The pinned benchmark revision publishes `hard_number_eval.jsonl`, so the production config now names that immutable file. Real corpus tar members are basename-only while the trusted JSON sidecar carries canonical `<shard>/<basename>` identities; indexing reconstructs and verifies that binding. The augmented per-shard manifests provide the current tar digest while `verification.json` retains the pre-augmentation source digest, so preparation validates both provenance layers and pins the current archive bytes from the augmented manifest.
- Decision: Empty combined text is accepted only for a ROVER row whose agreement is null and therefore excluded. The real sidecar contains 168 empty-text rows and 309 null-agreement exclusions; any eligible/non-null row with empty text remains a hard failure.
- Validation: Real `pin` verified model `OpenBMB/VoxCPM2@bffb3df5a29440629464e5e839f4d214c8714c3d`, benchmark `bitmanagerai/hard_number_eval_for_tts@57b964492ccfcedd6a24d0225ef4b7d3697ffdca`, and GigaAM `istupakov/gigaam-v3-onnx@322c3b29492673eb7d0b434bfa9dfb8653e34d02`. Real preparation completed in approximately two hours with 519 source shards, 4,075,032 joined rows, 309 null-agreement exclusions, 2,486,821 stage-1 rows, 1,587,902 stage-2 rows, 4 memorization samples, 20 prompts, and 2,000 assignments. The index fingerprint is `b276bdaf32e1ef2ed0275b919625215959e91494ad354caca8f272273f210cf2`; the selection fingerprint is `d7ab905d69f33a64c4c747dbf665eef6ed5c313cad6ac12534d9e3036991b629`.
- Validation: Production audit rehashed every pin and source and reported a 1,159,962,624-byte SQLite index. Independent SQLite integrity, count/ordinal, selection uniqueness/eligibility, assignment range, audio-log ID, and all 24 selected-WAV SHA-256 checks passed.
- Immutability: Before and after Phase B, `verification.json` remained 80,269 bytes with mtime `2026-07-29 22:14:42`, the combined sidecar remained 1,366,964,556 bytes with mtime `2026-07-31 22:56:02`, and the ROVER archive remained 375,137,790 bytes with mtime `2026-07-29 21:58:57`.
- Validation: The project-environment launcher passed the prescribed synthetic GPU contracts: one-rank train/checkpoint restore, eight-rank identical-gradient checkpoint restore with 32 unique samples and next position 32, and eight-rank validation with 32 unique items, per-rank ASR release, and exactly one aggregate/completion/tracking publication. The global `rtk accelerate` command selected Python 3.12 without the project package; the runbook now uses verified `rtk uv run accelerate` commands and has a regression guard.
- Changed: Synthetic smoke paths now finalize Accelerate via its public `end_training()` lifecycle after the final barrier. The eight-rank checkpoint and validation reruns retained their PASS results without the prior NCCL process-group teardown warning.
- Scope: Phase B did not run real memorization, create an approval, launch either training stage, publish artifacts, or mutate the proprietary corpus.
## 2026-08-03 - Remote recovery checkpoint before 12:00 UTC

- Status: Branch `balalaika-lora-training` is pushed through `16c8f67` (`fix: harden Balalaika qualification boundaries`). The tracked worktree was clean at 06:35 UTC before this note.
- Completed: Implemented the two-stage Accelerate LoRA pipeline, deterministic indexed data access, four-item memorization/manual approval gate, distributed validation, W&B metrics/audio logging, CLI/config/runbook, checkpoint recovery, and production audit workflow.
- Completed: Pinned with modern `hf` CLI: VoxCPM2 `bffb3df5…`, hard-number benchmark `57b96449…`, and GigaAM `322c3b29…`. The benchmark file is `hard_number_eval.jsonl`.
- Completed: The first successful production prepare/audit observed 519 shards, 4,075,032 joined rows, 309 null-agreement exclusions, stage 1 = 2,486,821 rows, stage 2 = 1,587,902 rows, four memorization samples, 20 prompts, and 2,000 benchmark assignments. The index was 1,159,962,624 bytes; index fingerprint began `b276bdaf` and selection fingerprint began `d7ab905d`.
- Completed: Real GPU smokes passed on 8× RTX 5090: one-rank and eight-rank train/checkpoint restore, plus eight-rank 32-item validation. Explicit Accelerate teardown removed the NCCL process-group warning.
- Incident: A workflow test accidentally targeted the production generated-output root and removed the generated `index/` and `selection/` directories. No proprietary corpus tar, ROVER file, combined sidecar, Hub pin, run output, or model data was changed. No recoverable generated-output backup remained; the artifacts are deterministically rebuildable from the immutable corpus.
- Hardened: Commit `16c8f67` prevents incomplete generations from committing, verifies canonical index/audit/hash/fingerprint and deep selection state before commit, rolls back the previous generation by same-filesystem rename, and forces checked-config workflow tests to isolated temporary roots.
- In progress at 06:35 UTC: Deterministic production rebuild under the hardened transaction. The last observed staging count was 475,657 / 4,075,032 source rows; no ROVER/combined/final rows were present and no incomplete generation had been published.
- Remaining before final handoff: allow the rebuild to finish; run the independent deep production audit; verify exact counts, pins, corpus/input hashes, SQLite integrity, selection identities/WAV hashes, and clean transaction state; run final focused/full tests and static checks; commit and push recovery evidence; complete Task 14 scoped re-review; run the whole-branch review and push any final reviewed fix.
- Operator-only follow-up: Do not run real `memorize`, create `approve`, or start stage training until the rebuilt artifacts pass deep audit and the operator is ready to inspect the dedicated W&B memorization run manually.

## 2026-08-03 - Recovery checkpoint update at 08:28 UTC

- Status: Remote branch is pushed through `e4d8999` (`fix: qualify pinned benchmark row schema`), based on the earlier handoff checkpoint `e12f32b` and transaction-safety checkpoint `16c8f67`.
- Rebuild result: The hardened recovery reached post-build generation validation, then failed safely because real benchmark rows contain additional metadata keys beyond the five fields consumed by training. The transaction rolled back completely: no index, selection, backup, or staging generation remains under `/workspace/balalaika_lora_training`; the immutable proprietary corpus metadata is unchanged.
- Fixed: Benchmark loading now explicitly projects and validates only `id`, `category`, `text`, `normalized_gold`, and `stressed`, while accepting unrelated pinned-dataset metadata such as `hard_number` and `why_hard`. The production-shaped RED and 47 focused tests passed before commit.
- Data safety: The generated index/selection are currently absent, but are deterministically recoverable from the verified immutable corpus and pinned Hub artifacts. No memorization, approval, stage training, W&B training run, model publication, or corpus mutation occurred.
- Remaining before final handoff: rerun production `prepare` from scratch under commit `e4d8999`; run the independent deep production `audit`; verify exact counts/fingerprints/pins/input hashes/SQLite integrity/selection identities and clean transaction state; run full tests/static checks; commit and push final recovery evidence; complete Task 14 scoped re-review and whole-branch review.
- Safety gate: Real `memorize`, `approve`, and `train --stage` remain prohibited until the rebuilt generation passes deep audit and the operator manually reviews the dedicated memorization W&B run.
